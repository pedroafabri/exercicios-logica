programa {

  // Nossa função principal do exercício
  funcao inteiro calculaAreaQuadrado(inteiro lado) {
    retorne lado * lado // Calculamos a área (lado * lado) e retornamos seu valor diretamente.
  }

  // Função início onde executaremos nossos testes
  funcao inicio() {
    escreva(calculaAreaQuadrado(2)) // Resultado esperado: 2 x 2 = 4
    escreva(calculaAreaQuadrado(4)) // Resultado esperado: 4 x 4 = 16
    escreva(calculaAreaQuadrado(8)) // Resultado esperado: 8 x 8 = 64
  }
}
